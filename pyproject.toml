[project]
name = "langchain-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "langchain>=1.3.14",
    "langchain-community>=0.4.2",
    "langchain-openrouter>=0.2.6",
    "streamlit>=1.59.2",
]

[tool.uv.workspace]
members = [
    "ai-chat-playground",
]

[tool.pyright]
extraPaths = [
    "Prompt Template/city_cuisine_planner"
]

